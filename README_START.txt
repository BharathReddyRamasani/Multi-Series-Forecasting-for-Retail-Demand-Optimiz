# DemandAI — Start Scripts
# Run these from d:\forecast\

# Backend (Terminal 1):
# python start_backend.py
# OR manually:
# cd backend && pip install -r requirements.txt && uvicorn main:app --reload --port 8000

# Frontend (Terminal 2):
# cd frontend && npm install && npm run dev

# ── PowerShell one-liners ──────────────────────────────────────────────
# Backend:  cd d:\forecast; python start_backend.py
# Frontend: cd d:\forecast\frontend; npm install; npm run dev
